# Bitwarden CLI

Sidecar for Falcon Passwords (`bw serve` → local REST API on 127.0.0.1).
Upstream: https://github.com/bitwarden/clients (CLI, GPL-3.0). Fetched by
`tools/fetch-sidecars.ps1` into `win/bw.exe` (not committed).
